import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    margin: 20,
  },

  button: {
    marginTop: 10,
  },

  helpText: {
    alignSelf: 'center',
    marginBottom: 10,
  },
});
